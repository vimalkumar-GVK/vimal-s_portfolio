Place your resume PDF here.

1. Save your resume as `Vimal_Kumar_Resume.pdf`.
2. Put the file in this `assets/` folder so the site link will work: `/assets/Vimal_Kumar_Resume.pdf`.

Notes:
- The "Resume" button on the homepage opens the PDF in a new tab.
- If you prefer the button to download the file instead of opening, remove `target="_blank"` and add the `download` attribute to the link in `index.html`.
